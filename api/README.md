## Concepts

### UUIDv7
- Time sortable

### Cursor-based pagination
- Better performance and efficiency when compared to an offset-based pagination
- Allows to use the latest id shown (cursor) to be used on WHERE clause and get only the values you need
